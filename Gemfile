# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'celluloid-io'
gem 'dentaku' # Math
gem 'dotenv' # Load the .env variables
gem 'puma', '~> 4.1'
gem 'slack-ruby-bot' # Provide API interface to interact with the bot
